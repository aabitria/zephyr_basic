description: Custom properties
compatible: "custom,props-basics"

properties:
  int:
    type: int
  string:
    type: string
    
# Template for every property binding    
#properties:
#  <property-name>:
#    type: <property-type>
#    # required: false -> omitted by convention if false

